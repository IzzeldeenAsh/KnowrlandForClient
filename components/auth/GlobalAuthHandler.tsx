// Kept for import compatibility. GlobalProfileProvider now owns session
// synchronization without forcing a round trip through the Angular application.
export default function GlobalAuthHandler() { return null; }
